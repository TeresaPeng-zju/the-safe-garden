# Keep this key server-side. Never prefix it with NEXT_PUBLIC_.
DEEPSEEK_API_KEY=

# DeepSeek's current fast non-thinking model is a good fit for the short,
# structured coaching rewrite. Override this only if your account uses another model.
DEEPSEEK_MODEL=deepseek-v4-flash
